# Pinned upstream snapshots

These four small files are verbatim snapshots from
`trmdy/zeta-simple-zeros-673137` at commit
`1610b97b7895ff34982260f8dcaf04a0f7b82cf7`:

- `data/candidate-retuned-p2736.json`;
- `data/candidate-nine-point-final.json`;
- `certificates/retuned-p2736-grid4000.txt`;
- `certificates/nine-point-final-grid4000.txt`.

They are copied here so that reviewers can inspect the exact weights,
capacities, window identity, and reported run metadata without following a
mutable branch. Their original Git blob identities are recorded in
`../UPSTREAM.lock`.

These snapshots are source and log records only. They do not include the
upstream interval tables or verifier and therefore do not constitute an
independent replay of either large certificate.
